interface:
  display_name: "Worktree 状態確認"
  short_description: "git worktree を削除可否ごとに安全に分類する"
  default_prompt: "Use $worktree-status-check to inspect local git worktrees and classify them into safe_to_remove, removal_candidate_with_leftovers, or in_progress."

